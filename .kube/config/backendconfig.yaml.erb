---
apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: config-default
  namespace: <%= namespace %>
spec:
  iap:
    enabled: <%= iap_enabled %>
    oauthclientCredentials:
      secretName: gcp-oauth-iap
      # n.b. The contents of gcp-oauth-iap are actually empty. It's here
      # because `kubectl apply` validates the data structure.
